import { Redirect } from 'expo-router';

// Since we use React Navigation manually, redirect everything to our App entry.
// This file satisfies expo-router's requirement for an _layout file.
export default function RootLayout() {
  return null;
}
